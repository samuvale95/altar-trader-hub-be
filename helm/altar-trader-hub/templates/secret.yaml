apiVersion: v1
kind: Secret
metadata:
  name: altar-secrets
  labels:
    {{- include "altar-trader-hub.labels" . | nindent 4 }}
type: Opaque
data:
  database-url: {{ .Values.postgresql.auth.database | b64enc }}
  redis-url: {{ "redis://redis:6379" | b64enc }}
  secret-key: {{ "your-secret-key-here" | b64enc }}
