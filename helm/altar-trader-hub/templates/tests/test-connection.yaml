apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "altar-trader-hub.fullname" . }}-test-connection"
  labels:
    {{- include "altar-trader-hub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "altar-trader-hub.fullname" . }}:{{ .Values.service.port }}']